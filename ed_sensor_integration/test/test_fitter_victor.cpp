#include <ed/kinect/fitter.h>

int main(int argc, char **argv)
{
    Fitter fitter;
    FitterData data;

    std::cout << "This is where the fitter shall be tested!!!" << std::endl;
    //fitter.processSensorData(image, sensor_pose, data);
    //fitter.estimateEntityPose(data, worldmodel, entity_id, expected_pose, fitted_pose)

    return 0;
}
